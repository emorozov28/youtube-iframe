import React from 'react';
import Button from '../button/button';

const App: React.FC = () => {
    return (
        <div>
            <h1>Вітаємо React-додатку з TypeScript!</h1>
            <Button />
        </div>
    );
};

export default App;
